# Managed by puppet - do not edit direct

# The loopback network interface
auto lo
iface lo inet loopback

# Dont't load it this way - if it fails network fails - that makes it's hard for puppet execution
# <%= loadfirewall %>

# The primary network interface
<%= auto0_stanza %>
<%= allow_hotplug0 %>
<%= iface0 %>
	<%= eth0_ip %>
	<%= eth0_netmask %>
	<%= eth0_network %>
	<%= bcstnet0 %>
	<%= gateway0 %>

<%= addroute_eth0 %>
<%= removeroute_eth0 %>

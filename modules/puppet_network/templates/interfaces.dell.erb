# Managed by puppet - do not edit direct

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
iface eth0 inet manual
<%= iface0 %>
	<%= eth0_ip %>
	<%= eth0_netmask %>
	<%= eth0_network %>
	<%= bcstnet0 %>
	<%= gateway0 %>

<%= addroute_eth0 %> 
<%= removeroute_eth0 %>
